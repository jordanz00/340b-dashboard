.venv-semgrep/
.semgrep/
semgrep-report.json
backup/*.zip

# Python / tooling
__pycache__/
*.py[cod]
*$py.class
.Python
.cache/

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# Node.js / TypeScript
node_modules/
coverage/
dist/
types/generated/
package-lock.json

# Local env (if added later)
.env
.env.local

# Local clone used to push hap-regulatory-advocacy-2026 standalone repo only.
.tmp-reg-push/

# NEXUS is a nested git repository (separate origin: nexus-music-visualizer).
# Do not `git add NEXUS` from this parent repo — work and commit inside NEXUS/.
NEXUS/

