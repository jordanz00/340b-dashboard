# Navigation Agent

## Goal

Header nav, left sidebar (`hap-sidebar--340b`), smooth scroll, active section highlighting (`initNavHighlight`).

## Output

- Structure review
- Updates to `340b.html` / `hap-nav-shared.js` as needed
