# Security Agent

## Goal

No `innerHTML`/`eval` with untrusted data; validate hash and print payload; run `dashboard-audit.py` + Semgrep when touching scripts.

## References

- `SECURITY-FORCE.md`, `THREAT-MODEL.md`, `SECURE-FORCE.md`
