.venv-semgrep/
.semgrep/
semgrep-report.json
backup/*.zip

# Python / tooling
__pycache__/
*.py[cod]
*$py.class
.Python
.cache/

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# Node.js / TypeScript
node_modules/
coverage/
dist/
types/generated/
package-lock.json

# Local env (if added later)
.env
.env.local

# NEXUS is a nested git repository (separate origin: nexus-music-visualizer).
# Do not `git add NEXUS` from this parent repo — work and commit inside NEXUS/.
NEXUS/

